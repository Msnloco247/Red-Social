@using Red_Social1.Core.Application.ViewModels.User
@model LoginViewModel
@{
    ViewData["Title"] = "Login";
}

<div class="row mt-5">
    <div class="col-6 offset-3">
        <div class="card shadow-sm" >
            <div class="card-body">
                @Html.ValidationSummary(false, "", new { @class = "text-danger" })
                @if (Model.HasError)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert"> 
                        <p>@Model.Error</p>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }
                <form method="post" asp-controller="User" asp-action="Index">

                    <h2 class="text-primary mb-3 text-center h1">FEISBU</h2>
                    <div class="mb-3">
                        <label asp-for="Email" class="form-label">Correo del usuario</label>
                        <input class="form-control" asp-for="Email">
                    </div>
                    <div class="mb-3">
                        <label asp-for="Password" class="form-label">Contraseña</label>
                        <input class="form-control" asp-for="Password">                        
                    </div>
                    <div class="d-grid mb-3">
                     <a asp-controller="User" asp-action="ForgotPassword" class=" me-2">Olvide mi contrasena</a>
                    <a asp-controller="User" asp-action="Register" class="me-2">Registrarse</a>
                    </div>
                    <button type="submit" class="btn btn-primary float-end">Iniciar sesion</button>

                </form>

            </div>
        </div>
    </div>
</div>

